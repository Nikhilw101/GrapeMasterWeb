# Server Configuration
PORT=5000
NODE_ENV=development

# Database


# JWT Configuration
JWT_SECRET=your_strong_jwt_secret_key_here
JWT_ACCESS_SECRET=your_strong_access_token_secret_here
JWT_REFRESH_SECRET=your_strong_refresh_token_secret_here
JWT_EXPIRE=7d
JWT_ACCESS_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# Email Configuration (Resend)
RESEND_API_KEY=re_123456789
EMAIL_FROM=onboarding@resend.dev
# For production: EMAIL_FROM=Grape Master <noreply@yourdomain.com>

# Frontend URL – where the React/Vite app is hosted (used for ALL email links + Stripe redirects).
# Local: http://localhost:3000
# Production (e.g. Vercel): https://grape-master.vercel.app
FRONTEND_URL=http://localhost:3000
# Optional overrides (defaults: FRONTEND_URL + path). Set in production so emails don't use localhost.
# RESET_PASSWORD_URL=https://grape-master.vercel.app/reset-password
# ADMIN_RESET_PASSWORD_URL=https://grape-master.vercel.app/admin/reset-password
# PAYMENT_SUCCESS_URL=https://grape-master.vercel.app/payment/success
# PAYMENT_CANCEL_URL=https://grape-master.vercel.app/payment/cancel

# ---- Initial admin (first-time setup only) ----
# Run POST /api/admin/seed once when no admins exist. After that, login is DB-only (no .env used for login).
INITIAL_ADMIN_EMAIL=
INITIAL_ADMIN_PASSWORD=
INITIAL_ADMIN_NAME=Admin
INITIAL_ADMIN_MOBILE=
# Optional: email to remove from DB when running scripts/seedAdmin.js (e.g. legacy account). Leave empty to skip.
# LEGACY_ADMIN_EMAIL_TO_REMOVE=

# ---- Optional fallbacks (overridable in Admin Settings) ----
# Admin email for order/dealer notifications (or set "adminEmail" in Admin Settings)
ADMIN_EMAIL=nwagh008@gmail.com
# Company info for email templates (or set companyName, companyAddress, companyPhone, companyEmail in Admin Settings)
COMPANY_NAME=Grape Master
COMPANY_ADDRESS=
COMPANY_PHONE=
COMPANY_EMAIL=


# Stripe Payment Gateway (Test Mode)
STRIPE_SECRET_KEY=sk_test_your_stripe_test_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Nodemailer (Fallback for Resend)
NODEMAILER_HOST=smtp.gmail.com
NODEMAILER_PORT=587
NODEMAILER_USER=
NODEMAILER_PASS=

# WhatsApp API Configuration
WHATSAPP_API_URL=your_whatsapp_api_url
WHATSAPP_API_KEY=your_whatsapp_api_key
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id

# Delivery Charges

